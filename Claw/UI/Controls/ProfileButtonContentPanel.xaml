<UserControl x:Class="Claw.UI.ProfileButtonContentPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="30" d:DesignWidth="100">
    <Grid HorizontalAlignment="Stretch">
        <Border BorderBrush="{x:Null}" Height="30" Margin="5,0,25,0">
            <TextBlock x:Name="tbName" VerticalAlignment="Center" TextWrapping="WrapWithOverflow"/>
        </Border>
        <Button VerticalAlignment="Center" Margin="0,0,5,0" HorizontalAlignment="Right" Width="15" Height="15" Click="OnCloseButtonClick">
            <Image Source="{DynamicResource CloseImage}"/>
        </Button>
    </Grid>
</UserControl>
